# CMake build output
build/
build_play/
build_manual/
TS3VASManagerServer/build/
TS3VASManagerServer/build_manual/
exe/build/
exe/build_manual/

# Compiled binaries (deployed to C:\ts3_tool\ by build scripts)
*.dll
*.exe
*.lib
*.exp
*.pdb
*.obj
*.ilk

# Visual Studio
.vs/
*.user
*.suo

# Logs and telemetry (written to C:\ts3_tool\ and E:\...)
*.log
*.csv
# published graph data — tracked on purpose
!docs/vas_comparison.csv
.claude/

# Scratch notes (local build-flavor reminders, not for the repo)
crash
lean

# Detours: users supply their own source from https://github.com/microsoft/detours
# (CMakeLists.txt errors with instructions if missing). Track only the placeholder.
extern/detours/src/*
!extern/detours/src/PLACE_DETOURS_HERE.txt
